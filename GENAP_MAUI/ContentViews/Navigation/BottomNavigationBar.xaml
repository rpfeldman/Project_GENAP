<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:GENAP_MAUI"
             x:Class="GENAP_MAUI.ContentViews.Navigation.BottomNavigationBar"
             x:Name="Root">

    <Grid HeightRequest="92" VerticalOptions="End">

        <Border VerticalOptions="End"
                HeightRequest="64"
                BackgroundColor="{AppThemeBinding Light={StaticResource CardLight}, Dark={StaticResource CardDark}}"
                Stroke="{AppThemeBinding Light={StaticResource BorderLight}, Dark={StaticResource BorderDark}}"
                StrokeThickness="1"
                Padding="0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="20,20,0,0" />
            </Border.StrokeShape>

            <Grid ColumnDefinitions="*,*,72,*,*">

                <VerticalStackLayout Grid.Column="0" Spacing="2" Padding="0"
                                     VerticalOptions="Center" HorizontalOptions="Center">
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnCategoriesTapped" />
                    </VerticalStackLayout.GestureRecognizers>
                    <Label Style="{StaticResource SymbolLabel}" x:Name="IconCategories" Text="{x:Static local:Symbols.CategoriesIcon}" HorizontalOptions="Center" FontFamily="Symbols"/>
                    <Label x:Name="TextCategories" Text="Categorías" FontSize="10" HorizontalOptions="Center" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="1" Spacing="2" Padding="0"
                                     VerticalOptions="Center" HorizontalOptions="Center">
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnDefaultTapped" />
                    </VerticalStackLayout.GestureRecognizers>
                    <Label Style="{StaticResource SymbolLabel}" x:Name="IconDefault" Text="{x:Static local:Symbols.RegistTransactionIcon}" HorizontalOptions="Center" FontFamily="Symbols" />
                    <Label x:Name="TextDefault" Text="Movimiento" FontSize="10" HorizontalOptions="Center" />
                </VerticalStackLayout>


                <VerticalStackLayout Grid.Column="3" Spacing="2" Padding="0"
                                     VerticalOptions="Center" HorizontalOptions="Center">
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnTradeTapped" />
                    </VerticalStackLayout.GestureRecognizers>
                    <Label Style="{StaticResource SymbolLabel}" x:Name="IconTrade" Text="{x:Static local:Symbols.RegistTradeIcon}" HorizontalOptions="Center" FontFamily="Symbols" />
                    <Label x:Name="TextTrade" Text="Trade" FontSize="10" HorizontalOptions="Center" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="4" Spacing="2" Padding="0"
                                     VerticalOptions="Center" HorizontalOptions="Center">
                    <VerticalStackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnGraphsTapped" />
                    </VerticalStackLayout.GestureRecognizers>
                    <Label Style="{StaticResource SymbolLabel}" x:Name="IconGraphs" Text="{x:Static local:Symbols.GraphsIcon}" HorizontalOptions="Center" FontFamily="Symbols" />
                    <Label x:Name="TextGraphs" Text="Gráficos" FontSize="10" HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Grid>
        </Border>

        <Border x:Name="FabBorder"
                WidthRequest="60" HeightRequest="60"
                VerticalOptions="Start" HorizontalOptions="Center"
                BackgroundColor="{StaticResource BrandViolet}"
                StrokeThickness="0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="30" />
            </Border.StrokeShape>
            <Border.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnDashboardTapped" />
            </Border.GestureRecognizers>
            <Label Style="{StaticResource SymbolLabel}" Text="{x:Static local:Symbols.MainDashboardIcon}" FontSize="26" HorizontalOptions="Center" VerticalOptions="Center" FontFamily="Symbols" />
        </Border>

    </Grid>

</ContentView>