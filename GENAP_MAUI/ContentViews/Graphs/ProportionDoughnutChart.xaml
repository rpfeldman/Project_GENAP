<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             x:Class="GENAP_MAUI.ContentViews.Graphs.ProportionDoughnutChart"
             x:Name="Root">

    <VerticalStackLayout Spacing="8">
        <Label x:Name="TitleLabel" Style="{StaticResource ChartTitle}" IsVisible="False" />

        <Grid HeightRequest="340">
            <Label Text="{Binding CenterText, Source={x:Reference Root}}"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   HorizontalTextAlignment="Center"
                   FontSize="16"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light={StaticResource TextPrimaryLight}, Dark={StaticResource TextPrimaryDark}}"
                   Margin="0,0,0,30"
                   InputTransparent="True" />

            <lvc:PieChart
                Series="{Binding DoughnutSeries, Source={x:Reference Root}}"
                LegendPosition="Bottom"
                BackgroundColor="Transparent" />
        </Grid>
    </VerticalStackLayout>

</ContentView>